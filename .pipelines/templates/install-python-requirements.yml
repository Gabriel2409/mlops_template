steps:
  - task: UsePythonVersion@0
    inputs:
      versionSpec: "3.10"
      addToPath: true
      architecture: "x64"
  - task: Bash@3
    inputs:
      targetType: "inline"
      script: |
        set -e # fail on error
        pip install -r requirements.txt